export enum FilterLevel {
  PROVIDER = 'provider',
  AREA = 'area',
}

export enum FilterAreaUnit {
  PROVINCE = 'province',
  DISTRICT = 'district',
  COMMUNE = 'commune',
}

export enum FilterTimeUnit {
  YEAR = 'year',
  QUARTER = 'quarter',
  MONTH = 'month',
}
